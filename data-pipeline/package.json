{
  "name": "data-pipeline",
  "version": "1.0.0",
  "author": "Flatten",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start-analytics": "node launcher.js ./prod-to-analytics",
    "lint": "eslint --fix .",
    "auth": "gcloud auth login && gcloud auth application-default login && gcloud config set project flatten-staging-271921",
    "auth-no-browser": "gcloud auth login && gcloud auth application-default login --no-launch-browser && gcloud config set project flatten-staging-271921"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^2.1.0",
    "digest-fetch": "^1.1.1",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^1.6.0",
    "dotenv": "^8.2.0",
    "eslint": "^7.0.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.3",
    "husky": "^4.2.5",
    "lint-staged": "^10.2.6",
    "prettier": "^2.0.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix"
  }
}
